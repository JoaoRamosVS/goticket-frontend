import type {
    EventMinDTO,
} from "@/features/admin/admin-events/types/event.types";

import type {
    LoginResponse,
} from "@/features/auth/types/auth.types";

export type {
    EventMinDTO,
};

export type {
    LoginResponse,
};